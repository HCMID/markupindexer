package edu.holycross.shot.mid.markupreader
import org.scalatest.FlatSpec

import edu.holycross.shot.cite._
import edu.holycross.shot.ohco2._


class TeiPersNameIndexerSpec extends FlatSpec {

  "The TeiPersNameIndexer object" should "recursively collect text from XML containers"  in  pending /*{
    val xml = "<div n=\"1\"><ab>Text 1</ab></div>"
    // expect trailing space so xml elements are
    // not concatenated
    val expected = "Text 1 "
    val actual = MidProseABReader.diplomatic(xml)
    assert(actual == expected)
  }*/




}
